# OS files
.DS_Store

# Private workspace (local only, not shared)
private/

# Workspace View
.obsidian/workspace.json

# Prototype source captures (saved platform HTML — local only, large/regenerable)
proto-skeleton/html refs/
